// Superseded by ServiceDialog (which supports both create and edit). Kept as a
// re-export for any existing imports.
export { AddServiceDialog, ServiceDialog } from '@/components/maintenance/ServiceDialog';
